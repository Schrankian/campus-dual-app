<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is on -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    <style name="Widget.Android.AppWidgetContainer" parent="android:Widget">
        <!-- <item name="android:id">@android:id/background</item> -->
        <item name="android:background">@drawable/widget_bg_dark</item>
    </style>
    <style name="TimetableWidget.TextView" parent="android:Widget">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/on_background_dark</item>
        <item name="android:textSize">15sp</item>
    </style>
    <style name="TimetableWidget.TextView.Primary" parent="TimetableWidget.TextView">
        <item name="android:textColor">@color/primary_dark</item>
    </style>
    <style name="TimetableWidget.TextView.Secondary" parent="TimetableWidget.TextView">
        <item name="android:textColor">@color/primary_light</item>
    </style>
    <style name="TimetableWidget.Divider.Primary" parent="android:Widget">
        <item name="android:background">@color/primary_dark</item>
    </style>
    <style name="TimetableWidget.Divider.Secondary" parent="android:Widget">
        <item name="android:background">@color/primary_light</item>
    </style>
    <style name="TimetableWidget.ReloadIcon" parent="android:Widget">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:src">@drawable/ic_reload_dark</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>
</resources>
